open Parsetree

let loc = Location.none

let pp_exp = Pprintast.expression
let pp_typ = Pprintast.core_type

let () =
  Format.printf "%a" Pprintast.expression [%expr 1 + 1];
  Format.printf "%a" Pprintast.core_type [%type: int];
  Format.printf "%a" pp_exp ([%expr 1 + 1]);
  Format.printf "%a" pp_typ ([%type : int])
